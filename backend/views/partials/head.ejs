<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title><%= title %> · Game Nock Admin</title>
<link rel="stylesheet" href="/admin-assets/admin.css">
</head>
<body>
<div class="shell">
  <nav class="side">
    <div class="brand">Game Nock<span>Admin</span></div>
    <a href="/admin/dashboard" class="<%= active === 'dashboard' ? 'on' : '' %>">Dashboard</a>
    <a href="/admin/leads" class="<%= active === 'leads' ? 'on' : '' %>">Leads</a>
    <a href="/admin/testimonials" class="<%= active === 'testimonials' ? 'on' : '' %>">Testimonials</a>
    <a href="/admin/work" class="<%= active === 'work' ? 'on' : '' %>">Work</a>
    <a href="/admin/insights" class="<%= active === 'insights' ? 'on' : '' %>">Insights</a>
    <form action="/admin/logout" method="post" class="logout"><button type="submit">Log out</button></form>
  </nav>
  <main class="content">
